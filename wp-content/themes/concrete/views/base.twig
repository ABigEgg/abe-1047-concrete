<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ theme.link }}/dist/main.css">
    <link href="https://fonts.cdnfonts.com/css/effra-trial" rel="stylesheet">
    <script src="//unpkg.com/alpinejs" defer></script>
    {# <link href="https://fonts.cdnfonts.com/css/effra-heavy" rel="stylesheet"> #}
    
    {{ function('wp_head')  }}

</head>
<body {{ function('body_class', 'antialiased') }} x-data="{ hamburger: false }" x-bind:class="hamburger ? 'overflow-y-hidden' : ''">

{# Navigation #}
<div class="absolute top-0 left-0 z-30 w-full h-full bg-cc-dark-grey" x-show="hamburger">
     <header class="container w-full mx-auto mt-8 max-w-container">
        <div class="flex items-center justify-between">
            <div class="w-[69px] h-[80px]">
                <div>
                    {% include 'svg/concrete.svg' %}
                </div>
            </div>
            <div class="w-8 h-6">
                <a href="#" x-on:click="hamburger = !hamburger" class="relative transition-opacity -top-1 hover:opacity-50">
                    {% include 'svg/cross.svg' %} 
                </a>
            </div>  
        </div>

        <div class="mt-20">
            <ul class="">
                {% set items = ['About Us', 'Our Work', 'Services', 'Start a Project'] %}

                {% for item in items %}
                    <li><a href="#" class="text-2xl leading-tight transition-opacity text-cc-white hover:opacity-50">{{ item }}</a></li>
                {% endfor %}
            </ul>
        </div>

        <div class="mt-20">
            <div>
                <a href="#" class="text-xl transition-opacity text-cc-white hover:opacity-50">hello@concretecreative.co.uk</a>
            </div>
            <div>
                <a href="#" class="text-xl transition-opacity text-cc-white hover:opacity-50">0141 724 0089</a>
            </div>
        </div>
    </header>
</div>

{# Top nav #}
<div class="absolute z-10 w-full">
    <header class="container w-full mx-auto mt-8 max-w-container">
        <div class="flex items-center justify-between">
            <div class="w-[69px] h-[80px]">
                <div>
                    {% include 'svg/concrete.svg' %}
                </div>
            </div>
            <div class="w-8 h-6">
                <a href="#" x-on:click="hamburger = !hamburger" class="transition-opacity hover:opacity-50">
                    {% include 'svg/hamburger.svg' %}
                </a>
            </div>  
        </div>
    </header>
</div>

{% block main %}


{% endblock %}

{# Footer #}
<footer>
    <div class="py-16 bg-cc-dark-grey">
        <div class="container mx-auto max-w-container">
            <div class="mb-16 max-w-64">
                <img src="{{ site.theme.link }}/svg/concrete-logo.svg">
            </div>
            <div class="grid grid-cols-2">
                <div>
                    <h3 class="text-2xl leading-[0.8] text-cc-white">Say hello</h3>
                </div>
                <div class="text-2md">
                    <h4 class="text-cc-white">Contact</h4>
                    <p class=" text-cc-mid-grey">hello@concretecreative.co.uk</p>
                    <p class="text-cc-mid-grey">0141 724 0089</p>

                    <h4 class="mt-8 text-cc-white">Follow</h4>
                    <div class="text-cc-mid-grey">
                        <a href="#">Twitter</a>
                        <a href="#">Instagram</a>
                        <a href="#">Linkedin</a>
                    </div>
                </div>
            </div>
            <div class="mt-16 text-xs text-cc-white">
                <p><a href="#">Terms &amp; Privacy Policy</a> &copy; {{ function('date', 'Y') }} Concrete Creative</p>
            </div>
        </div>
    </div>
</footer>


    
{{ function('wp_footer') }}

</body>
</html>