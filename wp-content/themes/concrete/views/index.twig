{% extends 'base.twig' %}

{% block main %}

{# Masthead #}
{% include 'partials/masthead.twig' with {
    background_image: post.thumbnail.src
} %}

{# Intro #}
<div>
    <div class="py-10 md:py-16 lg:py-20">
        <div class="container mx-auto max-w-container px-4 md:px-0">
            <div class="grid grid-cols-1 md:grid-cols-12 gap-8">
                <div class="md:col-span-9">
                    <h3 class="mb-4 text-xl md:text-2xl lg:text-3xl text-cc-dark-grey">{{ intro.heading }}</h3>
                    <p class="text-lg md:text-xl text-cc-mid-grey">{{ intro.text }}</p>
                </div>
                <div class="md:col-span-3">

                </div>
            </div>
        </div>
    </div>
</div>


{% include 'partials/latest-projects.twig' with {
    background: 'bg-cc-light-grey',
    title: 'Featured Projects',
    projects: featured_projects
} %}


{% endblock %}